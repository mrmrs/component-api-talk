import theme from './theme'

const moduleWrapperTheme = {
  radii: [
    theme.radii[0],
    theme.radii[1],
    theme.radii[2],
    theme.radii[3],
  ],
  borderStyle: [
    theme.borderStyle[0]
  ],
  borderWidth: [
    theme.borderWidth[1], 
  ],
  borderColor: theme.borderColor,
  colors: theme.colors,
  backgroundColors: theme.colors,

}

export default moduleWrapperTheme
